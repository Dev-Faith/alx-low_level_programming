#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - swaps values of two integers
 * @s: param
 * return: len
 */
int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);
}
